function greet(name) {
    const message = "Hello " + name;
    return message;
  }
  
  function greetAgain(name) {
    const msg = "Hello " + name;
    return msg;
  }
  
  console.log(greet("Alice"));
  